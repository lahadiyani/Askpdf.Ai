{
  "build": {
    "commands": [
      "pip install --upgrade pip",
      "pip install -r requirements.txt"
    ]
  },
  "deploy": {
    "startCommand": "flask run --host=0.0.0.0 --port=8000 --app main:main"
  }
}
